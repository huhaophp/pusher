app:
  name: pusher
  port: 8081
  env: local # local, dev, prod

redis:  
  host: localhost
  port: 6379
  password: ""
  db: 0

source:
  redis:
    - "topic1"
  kafka:
    - "topic2"
    - "topic3"

logger:
  level: debug

kafka:
  servers: "localhost"
  group_id: "consumer"
  auto_offset_reset: "latest"